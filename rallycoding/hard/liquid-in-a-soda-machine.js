// https://www.rallycoding.com/problems/168

// MINE
const solve = target => {
  // [200, 300, 400]

  let rest
  let extra = 5

  while (0 < target) {
    if (400 <= target && target <= 400 + extra) {
    }
  }

  return false
}
