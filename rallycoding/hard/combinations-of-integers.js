// https://www.rallycoding.com/problems/181

// MINE
const solve = (arr, k) => {
  const operators = ['+', '-', '*', '/']
  for(let i=0; i<arr.length-1; ++i) {
    for(let j=i+1; j<arr.length; ++j) {
      for(let op of operators) {
        if(arr[i])
      }
    }
  }
  return;
};
